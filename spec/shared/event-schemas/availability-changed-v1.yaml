$schema: https://json-schema.org/draft/2020-12/schema
title: AvailabilityChanged.v1
type: object
allOf:
  - $ref: ../common-schemas.yaml#/components/schemas/EventMetadata
  - type: object
    required: [eventType, eventVersion, availabilityId, quantity, state]
    properties:
      eventType: {const: AvailabilityChanged.v1}
      eventVersion: {const: 1}
      availabilityId: {type: string}
      quantity: {type: integer}
      state: {type: string}
