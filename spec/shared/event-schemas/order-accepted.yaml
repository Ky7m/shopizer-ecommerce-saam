$schema: https://json-schema.org/draft/2020-12/schema
title: OrderAccepted
type: object
required: [eventId, eventType, eventVersion, tenantId, storeId, orderId, occurredAt]
properties:
  eventId: {type: string}
  eventType: {const: OrderAccepted}
  eventVersion: {type: integer}
  tenantId: {type: string}
  storeId: {type: string}
  orderId: {type: integer}
  occurredAt: {type: string, format: date-time}
x-contract-status: GAP

