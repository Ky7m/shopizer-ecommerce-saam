$schema: https://json-schema.org/draft/2020-12/schema
title: OrderStatusChanged
type: object
allOf:
  - $ref: ../common-schemas.yaml#/components/schemas/EventMetadata
  - type: object
    required: [eventType, eventVersion, orderId, previousStatus, status, source]
    properties:
      eventType: {const: OrderStatusChanged}
      eventVersion: {const: 1}
      orderId: {type: integer}
      previousStatus: {type: string}
      status: {type: string}
      source: {type: string}
