$schema: https://json-schema.org/draft/2020-12/schema
title: PaymentRefundFailed
type: object
x-contract-status: GAP
description: Rules-only variant; the approved payment API catalog does not define a consumer or final publication policy.
allOf:
  - $ref: ../common-schemas.yaml#/components/schemas/EventMetadata
  - type: object
    required: [eventType, eventVersion, paymentIntentId, orderId, amount, currency, errorCode]
    properties:
      eventType: {const: PaymentRefundFailed}
      eventVersion: {const: 1}
      paymentIntentId: {type: string}
      orderId: {type: string}
      amount: {type: string}
      currency: {type: string}
      errorCode: {type: string}
