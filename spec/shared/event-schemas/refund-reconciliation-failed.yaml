$schema: https://json-schema.org/draft/2020-12/schema
title: RefundReconciliationFailed
type: object
allOf:
  - $ref: ../common-schemas.yaml#/components/schemas/EventMetadata
  - type: object
    required: [eventType, eventVersion, orderId, paymentIntentId, amount, currency, reason]
    properties:
      eventType: {const: RefundReconciliationFailed}
      eventVersion: {const: 1}
      orderId: {type: integer}
      paymentIntentId: {type: string}
      amount: {type: string}
      currency: {type: string}
      reason: {type: string}
