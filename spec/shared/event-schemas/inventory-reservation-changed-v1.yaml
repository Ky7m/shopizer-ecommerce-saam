$schema: https://json-schema.org/draft/2020-12/schema
title: InventoryReservationChanged.v1
type: object
allOf:
  - $ref: ../common-schemas.yaml#/components/schemas/EventMetadata
  - type: object
    required: [eventType, eventVersion, aggregateId, reservationId, quantity, state]
    properties:
      eventType: {const: InventoryReservationChanged.v1}
      eventVersion: {const: 1}
      aggregateId: {type: string}
      productId: {type: string}
      reservationId: {type: string}
      reservationKey: {type: string}
      quantity: {type: integer}
      state: {type: string}
