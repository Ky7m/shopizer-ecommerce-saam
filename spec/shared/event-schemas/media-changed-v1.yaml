$schema: https://json-schema.org/draft/2020-12/schema
title: MediaChanged.v1
type: object
required: [eventId, eventType, eventVersion, tenantId, storeId, aggregateId, occurredAt]
properties:
  eventId: {type: string}
  eventType: {const: MediaChanged.v1}
  eventVersion: {const: 1}
  tenantId: {type: string}
  storeId: {type: string}
  aggregateId: {type: string}
  occurredAt: {type: string, format: date-time}
  mediaId: {type: string}
  changeType: {type: string}
  providerReference: {type: string}

