$schema: https://json-schema.org/draft/2020-12/schema
title: InventoryReservationReleased.v1
type: object
allOf:
  - $ref: ../common-schemas.yaml#/components/schemas/EventMetadata
  - type: object
    required: [eventType, eventVersion, reservationId, reservationKey, productId, quantity, state]
    properties:
      eventType: {const: InventoryReservationReleased.v1}
      eventVersion: {const: 1}
      reservationId: {type: string}
      reservationKey: {type: string}
      productId: {type: string}
      quantity: {type: integer, minimum: 1}
      state: {const: Released}
